name: Run Go Tests (SQLite)

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - ready_for_review
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

# Cancel superseded runs on the same branch/PR. Pushes to main are never cancelled.
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.6"

      - name: Download dependencies
        run: go mod download

      - name: Install gotestsum
        run: go install gotest.tools/gotestsum@latest

      - name: Run tests
        run: go vet ./... && gotestsum --format github-action -- -race -v -count=1 -timeout 30m ./...
        working-directory: ./dbos
        env:
          DBOS_TEST_BACKEND: sqlite
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
